{
  "scripts": {
    "deploy": "npm run build && scp -r ./dist/* root@web.herewallet.app:/var/www/here-www",
    "build": "parcel build ./landing/index.pug",
    "start": "parcel ./landing/index.pug"
  },
  "devDependencies": {
    "@parcel/transformer-pug": "^2.8.0",
    "@parcel/transformer-sass": "^2.8.0",
    "parcel": "^2.8.0"
  }
}
